<div class="container">
    <div class="row">
        <div class="col-md-12">
            <h1>Sources</h1>
            <p>A list of people who are known sources for the Daily Bruin.</p>
            <table id="sourceTable" class="table table-striped">
                <thead>
                    <tr>
                        <th data-override="name">Name of Source</th>
                        <th data-override="title">Title</th>
                        <th data-override="org">Organization</th>
                        <th data-override="workPhone">Work Phone</th>
                        <th data-override="cellPhone">Cell Phone</th>
                        <th data-override="otherPhone">Other Phone</th>
                        <th data-override="workEmail">Work Email</th>
                        <th data-override="personalEmail">Personal Email</th>
                        <th data-override="notes">Notes/</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each response}}
                    <tr id="{{@index}}">
                        <td data-property="name">{{this.name}}</td>
                        <td data-property="title">{{this.title}}</td>
                        <td data-property="org">{{this.org}}</td>
                        <td data-property="workPhone">{{this.workPhone}}</td>
                        <td data-property="cellPhone">{{this.cellPhone}}</td>
                        <td data-property="otherPhone">{{this.otherPhone}}</td>
                        <td data-property="workEmail">{{this.workEmail}}</td>
                        <td data-property="personalEmail">{{this.personalEmail}}</td>
                        <td data-property="notes">{{this.notes}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>
</div>
<!--Table to JSON-->
<script src="/javascripts/jquery.tabletojson.min.js"></script>
<!--custom page JS-->
<script src="/javascripts/page.js"></script>